export interface Tickets {
    event_date: string,
    event_id: number,
    event_title: string,
    issued_at: string,
    location: string,
    paid_amount: number,
    row_name: string,
    seat_number: string,
    ticket_ref: string
}